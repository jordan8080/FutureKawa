# Dépendances de développement / tests (NON embarquées dans l'image de prod).
# Installation : pip install -r requirements.txt -r requirements-dev.txt
pytest==8.3.4
httpx==0.28.1        # requis par fastapi.testclient.TestClient
ruff==0.8.4          # linter Python (stage "Qualité" de la pipeline)
